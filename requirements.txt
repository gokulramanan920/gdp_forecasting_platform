fastapi==0.109.0
uvicorn[standard]==0.27.0

# Database
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
alembic==1.13.1

# Environment & Config
python-dotenv==1.0.1

# Data Processing
pandas==2.1.4
numpy==1.26.3

# Machine Learning
scikit-learn==1.4.0
xgboost==2.0.3
lightgbm==4.3.0
catboost==1.2.2

# Data Sources
wbdata==0.3.0
requests==2.31.0

# Visualization
matplotlib==3.8.2
seaborn==0.13.1
panel==1.4.5
statsmodels==0.14.1

# Auth / JWT
python-jose[cryptography]==3.3.0

# Statistics
scipy==1.11.4

# Optional but useful
python-multipart==0.0.6  # For FastAPI file uploads
pydantic==2.5.3  # FastAPI dependency, pin version
httpx==0.26.0  # For async HTTP requests (News API, Claude API)

# Model persistence
joblib==1.3.2

# RAG / Embeddings
sentence-transformers==2.7.0
pgvector==0.3.2                  # PostgreSQL vector extension Python client
faiss-cpu==1.8.0                 # Vector similarity search

# News API
newsapi-python==0.2.7            # if using NewsAPI specifically

# Google Auth (SSO + notifications)
google-auth==2.28.0
google-auth-oauthlib==1.2.0
google-auth-httplib2==0.2.0

# Background jobs / scheduling
apscheduler==3.10.4             

# Async DB (when FastAPI routes need async queries)
asyncpg==0.29.0
sqlalchemy[asyncio]==2.0.25      # already have sqlalchemy, just add the extra

# Testing
pytest==7.0.0
pytest-asyncio==0.23.4
httpx==0.26.0                    # already have this, doubles as test client

# Logging / monitoring
structlog==24.1.0                # structured logging, better than print statements in prod